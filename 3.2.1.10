# Prompt the user to enter a word
# and assign it to the user_word variable.
user_word = input ("Please enter your word: ")

user_word = user_word.upper()

for letter in user_word: #letter variable in the variable user_word
    
    if letter in ('A', 'E', 'I', 'O', 'U'): #we can not user if == vowels as it is nonesense, it can only be == to one vowel or we can use if and in 
        continue
    print (letter)
    # Complete the body of the for loop.
